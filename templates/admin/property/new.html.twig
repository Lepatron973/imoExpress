{% extends 'base.html.twig' %}

{% block title %}
{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center">Créer un bien</h1>
        {{ form_start(form) }}
        <div class="row">
            <div class="col-6">{{form_row(form.title)}}</div>
        </div>
            {{form_rest(form)}}
            <button class="btn btn-primary">save</button>
        {{ form_end(form) }}
    </div>
{% endblock %}